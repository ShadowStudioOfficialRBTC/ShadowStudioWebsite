# 1. Stage all the files and updates in your ShadowStudios folder
git add .

# 2. Commit the new/updated website files
git commit -m "Update complete website with AI integration"

# 3. Push the new commit to GitHub
git push origin main

# Vercel model server setup
# Set SHADOW_MODEL_SERVER in Vercel to the public URL of the Python model server.
# Keep model_server.py running on a host that supports PyTorch and Qwen, then redeploy.